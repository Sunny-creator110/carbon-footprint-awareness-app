import '@testing-library/jest-dom';
import { beforeAll, afterAll, afterEach } from 'vitest';

// Set up clean window sizes/stubs if needed
beforeAll(() => {
  // Mock simple elements if needed
});

afterEach(() => {
  // Any cleanups
});

afterAll(() => {
  // Clear any global mock
});
